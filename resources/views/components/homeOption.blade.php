<div class="card">
    <div class="card-body">
        <div class="btn-group btn-group-lg w-100">
            <a href="#" class="btn btn-primary border" id="user_name_change_button">{{ __('タグ') }}</a>
            <a href="#" class="btn btn-primary border">{{ __('ユーザ') }}</a>
            <a href="#" class="btn btn-primary border" id="password_change_button">{{ __('応募順') }}</a>
            <a href="/newPost" class="btn btn-primary border">{{ __('新規投稿') }}</a>
        </div>
    </div>
</div>